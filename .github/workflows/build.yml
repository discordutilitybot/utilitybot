name: Build
on:
  push:
    branches:
    - dev

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps: 
      - name: Pull code
        uses: actions/checkout@main


      - name: Run
        run: | 
          python bot.py


      